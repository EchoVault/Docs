# SETRANGE

<Page 
  syntax={`SETRANGE key offset value`}
  module={"string"}
  categories={["string", "write", "slow"]}
  description={`Overwrites part of a string value with another by offset. Creates the key if it doesn't exist.`}
  options={[]}
  examples={{
    cli: [{ description: "", code: `SETRANGE key 2 "replacement string"` }],
    go: [{
      description: "",
      code: `vault, err := echovault.NewEchoVault()
if err != nil {
  log.Fatal(err)
}
length, err := vault.SetRange("key", 2, "replacement string")
`
    }]
  }}
/>