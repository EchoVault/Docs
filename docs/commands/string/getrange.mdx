# GETRANGE

<Page 
  syntax={`GETRANGE key start end`}
  module={"string"}
  categories={["string", "read", "slow"]}
  description={`Returns a substring from the string value.`}
  options={[]}
  examples={{
    cli: [{ description: "", code: `GETRANGE key 4 10` }],
    go: [{
      description: "",
      code: `vault, err := echovault.NewEchoVault()
if err != nil {
  log.Fatal(err)
}
length, err := vault.GetRange("key", 4, 10)
`
    }]
  }}
/>