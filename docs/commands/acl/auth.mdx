# AUTH

<Page 
  syntax={`AUTH [username] password`}
  module={"acl"}
  categories={["connection", "slow"]}
  description={`Authenticates the connection. If the username is not provided, the connection will be authenticated against the
default ACL user. Otherwise, it is authenticated against the ACL user with the provided username.`}
  options={[]}
  examples={{
    cli: [
      {
        description: "Authenticate against the default user:",
        code: `AUTH password`
      },
      {
        description: "Authenticate against a specific user:",
        code: `AUTH username password`
      }
    ],
    go: [{ description: "Not available in embedded mode." }]
  }}
/>