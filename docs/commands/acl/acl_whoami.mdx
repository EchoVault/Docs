# ACL WHOAMI

<Page 
  syntax={`ACL WHOAMI`}
  module={"acl"}
  categories={["fast"]}
  description={`Returns the authenticated user of the current connection.`}
  options={[]}
  examples={{
    cli: [{ description: "", code: `ACL WHOAMI`}],
    go: [{ description: "Not available in embedded mode." }]
  }}
/>