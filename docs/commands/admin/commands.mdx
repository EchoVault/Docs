# COMMANDS

<Page 
  syntax={`COMMANDS`}
  module={"admin"}
  categories={["admin", "slow"]}
  description={`Get a list of all the commands in available on the echovault with categories and descriptions.
  Sub-commmands are formated as "command|subcommand".`}
  options={[]}
  examples={{
    cli: [{ description: "", code: `COMMANDS` }],
    go: [{description: "Make use of the CommandList method."}]
  }}
/>