# ZMSCORE

<Page
  syntax={`ZMSCORE key member [member ...]`}
  module={"sortedset"}
  categories={["sortedset", "read", "fast"]}
  description={`Returns the associated scores of the specified member in the sorted set. 
Returns nil for members that do not exist in the set.`}
  options={[]}
  examples={{
    cli: [{description: "", code: `ZMSCORE key member1 member2`}],
    go: [{
      description: "",
      code: `vault, err := echovault.NewEchoVault()
if err != nil {
  log.Fatal(err)
}
scores, err := vault.ZMScore("key", "member1", "member2")
`
    }]
  }}
/>