# ZPOPMAX

<Page
  syntax={`ZPOPMAX key [count]`}
  module={"sortedset"}
  categories={["sortedset", "write", "slow"]}
  description={`Removes and returns 'count' number of members in the sorted set with the highest scores. 
Default count is 1.`}
  options={[]}
  examples={{
    cli: [{description: "", code: `ZPOPMAX key 2`}],
    go: [{
      description: "",
      code: `vault, err := echovault.NewEchoVault()
if err != nil {
  log.Fatal(err)
}
	members, err := vault.ZPopMax("key", 2)
`
    }]
  }}
/>