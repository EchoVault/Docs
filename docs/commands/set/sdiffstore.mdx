# SDIFFSTORE

<Page 
  syntax={`SDIFFSTORE destination key [key...]`}
  module={"set"}
  categories={["set", "write", "slow"]}
  description={`Works the same as SDIFF but stores the result at 'destination'.
Returns the cardinality of the new set.`}
  options={[]}
  examples={{
    cli: [{ description: "", code: `SDIFFSTORE destination key1 key2` }],
    go: [{
      description: "",
      code: `vault, err := echovault.NewEchoVault()
if err != nil {
  log.Fatal(err)
}
cardinality, err := vault.SDiffStore("destination", "key1", "key2")
`
    }]
  }}
/>