# SUNION

<Page 
  syntax={`SUNION key [key...]`}
  module={"set"}
  categories={["set", "read", "slow"]}
  description={`Returns the members of the set resulting from the union of the provided sets.`}
  options={[]}
  examples={{
    cli: [{ description: "", code: `SUNION key1 key2` }],
    go: [{
      description: "",
      code: `vault, err := echovault.NewEchoVault()
if err != nil {
  log.Fatal(err)
}
elements, err := vault.SUnion("key1", "key2")
`
    }]
  }}
/>