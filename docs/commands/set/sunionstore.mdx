# SUNIONSTORE

<Page 
  syntax={`SUNIONSTORE destination key [key...]`}
  module={"set"}
  categories={["set", "write", "slow"]}
  description={`Stores the union of the given sets into destination.`}
  options={[]}
  examples={{
    cli: [{ description: "", code: `SUNIONSTORE destination key1 key2` }],
    go: [{
      description: "",
      code: `vault, err := echovault.NewEchoVault()
if err != nil {
  log.Fatal(err)
}
cardinality, err := vault.SUnionStore("destination", "key1", "key2")
`
    }]
  }}
/>