# MGET

<Page
  syntax={`MGET key [key ...]`}
  module={"generic"}
  categories={['read', 'fast']}
  description={`Get multiple values from the specified keys.`}
  options={[]}
  examples={{
    cli: [{description: "Get multiple values:", code: `GET key1 key2 key3` }],
    go: [
      {
        description: "Get multiple values:",
        code: `ev, _ := echovault.NewEchoVault()
values, _ := ev.MGet("key1", "key2", "key3")`
      }
    ]
  }}
/>