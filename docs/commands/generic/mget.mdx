# MGET

<Page
  syntax={`MGET key [key ...]`}
  module={"generic"}
  categories={['read', 'fast']}
  description={`Get multiple values from the specified keys.`}
  options={[]}
  examples={{
    cli: [{description: "", code: `GET key1 key2 key3` }],
    go: [
      {
        description: "",
        code: `vault, err := echovault.NewEchoVault()
if err != nil {
  log.Fatal(err)
}
values, err := vault.MGet("key1", "key2", "key3")`
      }
    ]
  }}
/>