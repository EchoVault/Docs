# TTL

<Page
  syntax={`TTL key`}
  module={"generic"}
  categories={['keyspace', 'read', 'fast']}
  description={`Returns the remaining time to live for a key that has an expiry time in seconds.
If the key exists but does not have an associated expiry time, -1 is returned.
If the key does not exist, -2 is returned.`}
  options={[]}
  examples={{
    cli: [{description:"", code: `TTL key`}],
    go: [
      {
        description: "",
        code: `vault, err := echovault.NewEchoVault()
if err != nil {
  log.Fatal(err)
}
ttl, err := vault.TTL("key")`
      }
    ]
  }}
/>