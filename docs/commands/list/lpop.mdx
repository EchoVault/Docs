# LPOP

<Page 
  syntax={`LPOP key`}
  module={"list"}
  categories={["list", "write", "fast"]}
  description={`Removes and returns the first element of a list.`}
  options={[]}
  examples={{
    cli: [{ description: "", code: `LPOP key` }],
    go: [{
      description: "",
      code: `vault, err := echovault.NewEchoVault()
if err != nil {
  log.Fatal(err)
}
element, err := vault.LPop("key")
`
    }]
  }}
/>