# LPUSHX

<Page 
  syntax={`LPUSHX key element [element ...]`}
  module={"list"}
  categories={["list", "write", "fast"]}
  description={`Prepends a value to the beginning of a list only if the list exists.`}
  options={[]}
  examples={{
    cli: [{ description: "", code: `LPUSHX key element1 element2` }],
    go: [{
      description: "",
      code: `vault, err := echovault.NewEchoVault()
if err != nil {
  log.Fatal(err)
}
length, err := vault.LPushX("key", "element1", "element2")
`
    }]
  }}
/>