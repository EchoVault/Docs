# LTRIM

<Page 
  syntax={`LTRIM key start end`}
  module={"list"}
  categories={["list", "write", "slow"]}
  description={`Trims a list using the specified range.`}
  options={[]}
  examples={{
    cli: [{ description: "", code: `LTRIM key 2 6` }],
    go: [{
      description: "",
      code: `vault, err := echovault.NewEchoVault()
if err != nil {
  log.Fatal(err)
}
ok, err := vault.LTrim("key", 2, 6)
`
    }]
  }}
/>