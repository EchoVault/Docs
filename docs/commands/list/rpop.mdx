# RPOP

<Page 
  syntax={`RPOP key`}
  module={"list"}
  categories={["list", "write", "fast"]}
  description={`Removes and returns the last element of a list.`}
  options={[]}
  examples={{
    cli: [{ description: "", code: `RPOP key` }],
    go: [{
      description: "",
      code: `vault, err := echovault.NewEchoVault()
if err != nil {
  log.Fatal(err)
}
element, err := vault.RPop("key")
`
    }]
  }}
/>