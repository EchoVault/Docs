# HGETALL

<Page 
  syntax={`HGETALL key`}
  module={"hash"}
  categories={["hash", "read", "slow"]}
  description={`Get all fields and values of a hash.`}
  options={[]}
  examples={{
    cli: [{ description: "", code: `HGETALL key` }],
    go: [{
      description: "",
      code: `vault, err := echovault.NewEchoVault()
if err != nil {
  log.Fatal(err)
}
result, err := vault.HGetAll("key")`
    }]
  }}
/>