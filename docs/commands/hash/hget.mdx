# HGET

<Page 
  syntax={`HGET key field [field ...]`}
  module={"hash"}
  categories={["hash", "read", "fast"]}
  description={`Retrieve the value of each of the listed fields from the hash.`}
  options={[]}
  examples={{
    cli: [{ description: "", code: `HGET key field1 field2` }],
    go: [{
      description: "",
      code: `vault, err := echovault.NewEchoVault()
if err != nil {
  log.Fatal(err)
}
values, err := vault.HGet("key", "field1", "field2", "field3")`
    }]
  }}
/>